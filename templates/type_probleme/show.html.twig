{% extends 'base.html.twig' %}

{% block title %}TypeProbleme{% endblock %}

{% block body %}
    <h1>TypeProbleme</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_probleme.id }}</td>
            </tr>
            <tr>
                <th>Probleme</th>
                <td>{{ type_probleme.probleme }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_probleme_index') }}">back to list</a>

    <a href="{{ path('type_probleme_edit', {'id': type_probleme.id}) }}">edit</a>

    {{ include('type_probleme/_delete_form.html.twig') }}
{% endblock %}
