{% extends 'base.html.twig' %}

{% block title %}MatierePremiere{% endblock %}

{% block body %}
    <h1>MatierePremiere</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ matiere_premiere.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ matiere_premiere.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ matiere_premiere.description }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td>{{ matiere_premiere.photo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('matiere_premiere_index') }}">back to list</a>

    <a href="{{ path('matiere_premiere_edit', {'id': matiere_premiere.id}) }}">edit</a>

    {{ include('matiere_premiere/_delete_form.html.twig') }}
{% endblock %}
